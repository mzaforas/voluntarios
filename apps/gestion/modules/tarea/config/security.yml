# You can find more information about this file on the symfony website:
# http://www.symfony-project.org/reference/1_4/en/08-Security

all:
  credentials: admin

index:
  credentials: consulta

filter:
  credentials: consulta

ListFicha:
  credentials: consulta

generarPdf:
  credentials: consulta

DesasignarEquipo:
  credentials: tarea_asignar

AsignarEquipo:
  credentials: tarea_asignar

new:
  credentials: tarea_crear

create:
  credentials: tarea_crear

edit:
  credentials: tarea_asignar

update:
  credentials: tarea_asignar

delete:
  credentials: tarea_asignar

calendario:
  is_secure: false
